---
- name: Create
  hosts: localhost
  connection: local
  gather_facts: false
  no_log: "{{ molecule_no_log }}"
  tasks:
    - name: Create via kubevirt
      ansible.builtin.include_role:
        name: ipa-big.molecule_kubevirt
        tasks_from: create.yml
